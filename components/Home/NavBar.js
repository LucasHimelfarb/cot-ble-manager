import React, { memo } from 'react';
import {
    StyleSheet,
    Text,
    View,
} from 'react-native';

const NavBar = () => {
    return (
        <View>
            <Text>NavBar</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default memo(NavBar);
